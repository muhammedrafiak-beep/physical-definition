# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

sprite-work/
src/*.backup

sprite-work/
src/*.backup

# Scratch that `git add -A` in push.cmd was sweeping into the repo:
# reference GIFs, contact sheets, old patches. None of it is part of the
# app, the repo is public, and anything committed stays in the history.
Claude outputs/
scratchpad/
